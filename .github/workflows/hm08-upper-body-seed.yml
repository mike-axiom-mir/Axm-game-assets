name: hm08-upper-body-seed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  derive-upper-body:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Unit-test upper-body extraction
        run: python tools/bootstrap_hm08_upper_body_test.py

      - name: Download pinned CC0 hm08 source data
        env:
          MH_REV: a8bc2d54ff0ac92e78ff71431b1023eda42bf482
          MPFB_REV: 437dd513888a92399d1d3200d2e80859fae55abc
        run: |
          set -euo pipefail
          rm -rf build/hm08-upper-source build/hm08-upper-body-seed
          mkdir -p build/hm08-upper-source
          curl --fail --location --retry 3 \
            "https://raw.githubusercontent.com/makehumancommunity/makehuman/${MH_REV}/makehuman/data/3dobjs/base.obj" \
            -o build/hm08-upper-source/base.obj
          curl --fail --location --retry 3 \
            "https://raw.githubusercontent.com/makehumancommunity/mpfb2/${MPFB_REV}/src/mpfb/data/mesh_metadata/hm08_config.json" \
            -o build/hm08-upper-source/hm08_config.json
          for target in \
            "nose/nose-width1-incr.target" \
            "cheek/l-cheek-bones-incr.target" \
            "cheek/r-cheek-bones-incr.target"
          do
            out="build/hm08-upper-source/$(basename "$target")"
            curl --fail --location --retry 3 \
              "https://raw.githubusercontent.com/makehumancommunity/makehuman/${MH_REV}/makehuman/data/targets/${target}" \
              -o "$out"
          done
          test "$(git hash-object build/hm08-upper-source/base.obj)" = "d26635e9326e3cca30778fd7b9c00062b03cce09"
          test "$(git hash-object build/hm08-upper-source/hm08_config.json)" = "f94e2731356232ed1b6306128fd04d18da5d21ea"

      - name: Derive continuous head-neck-torso source
        env:
          MH_REV: a8bc2d54ff0ac92e78ff71431b1023eda42bf482
          MPFB_REV: 437dd513888a92399d1d3200d2e80859fae55abc
        run: |
          python tools/bootstrap_hm08_upper_body.py \
            --base-obj build/hm08-upper-source/base.obj \
            --config-json build/hm08-upper-source/hm08_config.json \
            --target build/hm08-upper-source/nose-width1-incr.target \
            --target build/hm08-upper-source/l-cheek-bones-incr.target \
            --target build/hm08-upper-source/r-cheek-bones-incr.target \
            --required-head-source-map seed_data/hm08_head_v0.2/source-index-map.json \
            --output build/hm08-upper-body-seed \
            --makehuman-revision "$MH_REV" \
            --mpfb-revision "$MPFB_REV"

      - name: Audit continuous source topology and head inclusion
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          from native_geometry import read_obj, topology_report
          from native_targets import load_target, validate_target

          root = Path("build/hm08-upper-body-seed")
          manifest = json.loads((root / "seed-manifest.json").read_text())
          assert all(manifest["acceptance"].values()), manifest["acceptance"]
          relation = manifest["relationship_to_head_seed"]
          assert relation["head_source_subset"] is True, relation
          assert relation["missing_head_source_vertices"] == [], relation

          mesh = read_obj(root / "upper_body.obj")
          topology = topology_report(mesh)
          assert topology["invalid_indices"] == 0, topology
          assert topology["degenerate_faces"] == 0, topology
          assert topology["nonmanifold_edges"] == 0, topology
          assert topology["boundary_edges"] > 0, topology
          assert len(mesh.vertices) > 4197
          assert len(mesh.faces) > 4168

          for target_path in sorted((root / "targets").glob("*.target")):
              target = load_target(target_path, license="CC0-source-remap")
              assert target.metadata["basemesh"] == "axm-hm08-upper-body-v0.1"
              report = validate_target(target, vertex_count=len(mesh.vertices))
              assert report["status"] == "pass", report
              assert report["rows"] > 0

          summary = {
              "vertices": len(mesh.vertices),
              "faces": len(mesh.faces),
              "boundary_edges": topology["boundary_edges"],
              "head_source_vertices": relation["required_head_source_vertices"],
              "vertical_slab_y": manifest["extraction"]["vertical_slab_y"],
          }
          (root / "audit-summary.json").write_text(json.dumps(summary, indent=2, sort_keys=True) + "\n")
          print("REAL HM08 UPPER BODY PASS", summary)
          PY

      - name: Preserve upper-body seed evidence
        uses: actions/upload-artifact@v4
        with:
          name: hm08-upper-body-seed
          if-no-files-found: error
          path: |
            build/hm08-upper-body-seed/upper_body.obj
            build/hm08-upper-body-seed/source-index-map.json
            build/hm08-upper-body-seed/seed-manifest.json
            build/hm08-upper-body-seed/audit-summary.json
            build/hm08-upper-body-seed/targets/*.target
